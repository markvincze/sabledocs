<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>gRPC documentation</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
  </head>
  <body>
  <section class="section">
    <div class="container">
      {% for package in packages %}
        <h3 class="title is-3">
          Package <code>{{ package.name }}</code>
        </h3>
        <div>
            {{ package.description }}
        </div>
        {% for enum in package.enums %}
          <h4 class="title is-4">
            Enum <code>{{ enum.name }}</code>
            <span class="subtitle is-6">(<code>{{ enum.full_name }}</code>)</span>
          </h4>
          <p>{{ description }}</p>
          <h5 class="title is-5">Values</h5>
          <table class="table">
            <thead>
              <tr>
                <th>Name</th>
                <th>Number</th>
                <th>Description</th>
              </tr>
            </thead>
            <tbody>
              {% for enum_value in enum.values %}
              <tr>
                <td>{{ enum_value.name }}</td>
                <td>{{ enum_value.number }}</td>
                <td>{{ enum_value.description }}</td>
              </tr>
              {% endfor %}
            </tbody>
          </table>
        {% endfor %}
      {% endfor %}
    </div>
  </section>
  </body>
</html>